import React from 'react';
import SimpleHero from './hero/SimpleHero';

/**
 * HeroSection
 * -----------
 * On-load CSS/Framer animation hero for all users. The previous scroll-locked
 * cinematic variant has been retired in favor of a single, consistent entrance.
 */
const HeroSection: React.FC = () => <SimpleHero />;

export default HeroSection;
